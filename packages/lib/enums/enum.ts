export enum UserRole {
    ADMIN = 'admin',
    USER = 'user',
    TEACHER = 'teacher',
    ASSISTANT = 'assistant',
    STUDENT = 'student',
}

export enum UserStatus {
    PENDING = 'pending',
    RESTRICTED = 'restricted',
    ALLOWED = 'allowed',
}